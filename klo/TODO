TODO LIST

    * <h1>...</h1> et al. should get proper names (for use with
      <a href="...#NAME">...</a>)
    * typographical " and ' in links (see e.g. index.html)
    * index.html should have blank lines between grammar & dictionary &
      additional parts
    * fix link in page footer to CSS validator
    * verify CSS against w3c validator
    * verify (X?)HTML against w3c validator
    * check for dead links
